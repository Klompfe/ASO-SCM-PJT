// 이행률(fulfillmentRate) 가중치를 별도 상수로 분리한다(PR-067) — 공정진행률과
// 출고율 중 무엇을 더 중요하게 볼지는 운영 중 조정될 여지가 크므로, 계산 로직
// 안에 파묻지 않고 여기서 한눈에 바꿀 수 있게 한다. 합이 1이 아니어도 계산은
// 되지만(가중합을 그대로 백분율로 씀), 의도한 대로 쓰려면 합을 1로 맞춰야 한다.
export const FULFILLMENT_WEIGHTS = {
  process: 0.7, // 공정진행률(재단/봉제/포장 평균) 가중치
  shipping: 0.3, // 출고율 가중치
};
